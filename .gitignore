# ============================================================
# Android / Gradle 项目忽略规则
# 原则：只提交源码与构建配置；凡是可由构建过程重新生成的产物、
#       与本机环境绑定的文件、IDE 个人配置、敏感凭据，一律不入库。
# 注意：本文件必须自我完备，不可依赖开发者本机的全局 gitignore，
#       否则 Jenkins 与其他成员的机器上规则会失效。
# ============================================================

# ------------------------------------------------------------
# Gradle 构建产物与本地缓存（均可重新生成）
# ------------------------------------------------------------
.gradle/
build/
captures/
.cxx/

# ------------------------------------------------------------
# 本机环境配置
# local.properties 内含本机 SDK 绝对路径，提交后会覆盖 Jenkins
# 节点的 SDK 位置，导致构建失败，必须忽略
# ------------------------------------------------------------
local.properties

# ------------------------------------------------------------
# 编译与打包产物
# ------------------------------------------------------------
# Compiled class file
*.class
*.dex
*.ap_
*.apk
*.aab

# ------------------------------------------------------------
# 日志文件与虚拟机崩溃转储
# ------------------------------------------------------------
# Log file
*.log
# virtual machine crash logs, see http://www.java.com/en/download/help/error_hotspot.xml
hs_err_pid*
replay_pid*

# ------------------------------------------------------------
# 签名密钥与凭据（严禁入库，改由 Jenkins 凭据管理在构建时注入）
# ------------------------------------------------------------
*.jks
*.keystore
keystore.properties

# ------------------------------------------------------------
# 归档文件
# 例外：gradle-wrapper.jar 是 ./gradlew 的启动器，必须入库，
#       否则 Jenkins 拉取代码后无法执行 Gradle 构建
# ------------------------------------------------------------
# Package Files #
*.jar
!gradle/wrapper/gradle-wrapper.jar
*.war
*.nar
*.ear
*.zip
*.tar.gz
*.rar

# ------------------------------------------------------------
# IDE 与编辑器的个人配置
# ------------------------------------------------------------
.idea/
*.iml
*.iws
*.ipr
.vscode/

# ------------------------------------------------------------
# 操作系统生成的文件
# ------------------------------------------------------------
.DS_Store
._*
Thumbs.db

# ------------------------------------------------------------
# Kotlin Gradle plugin data, see https://kotlinlang.org/docs/whatsnew20.html#new-directory-for-kotlin-data-in-gradle-projects
# ------------------------------------------------------------
.kotlin/

# ------------------------------------------------------------
# BlueJ files
# ------------------------------------------------------------
*.ctxt

# ------------------------------------------------------------
# Mobile Tools for Java (J2ME)
# ------------------------------------------------------------
.mtj.tmp/

# ------------------------------------------------------------
# AI 协作资产的目录约定（Harness Engineering）
# .jack/  个人工作区：Jack 的临时配置、草稿、未定稿的方案，不入库
# .ai/    团队共享区：定稿后的架构方案、协作规范、Skill，必须入库
#         （否则同事与 CI 拉不到 AGENTS.md 所引用的文档，
#           AI 在不同机器上的上下文就不一致，「记忆外化」失效）
# 迁移动作：文档在 .jack/ 中迭代，评审通过后再迁入 .ai/ 转为团队版
# 例外：.ai/context/current-task.md 是个人当前进度，每次中断都改写，
#       入库只会制造无意义的 diff 噪音
# ------------------------------------------------------------
.jack/
/.ai/context/current-task.md

# ------------------------------------------------------------
# 本地辅助脚本（个人工作流工具，非项目构建产物）
# ------------------------------------------------------------
/final_review_gate.py

# ------------------------------------------------------------
# 道具 3D 模型目录必须入库
# 不少人的全局 gitignore（~/.gitignore_global）会有一条裸 `models`
# 用来挡机器学习权重，它会连这里的 GLB 一起吃掉。漏掉素材不会报错，
# 只会静默降级成静态卡面，排查起来很费劲，所以在项目里显式豁免。
# ------------------------------------------------------------
!app/src/main/assets/models/
!app/src/main/assets/models/**
